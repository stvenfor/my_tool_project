/** Static hold: first frame shows complete report (for thumbnail/preview). */
export const HOLD_FRAMES = 45;

/** Animation duration after hold. */
export const ANIM_FRAMES = 180;

export const TOTAL_FRAMES = HOLD_FRAMES + ANIM_FRAMES;
